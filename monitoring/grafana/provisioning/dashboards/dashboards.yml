# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'OnQuota Dashboards'
    orgId: 1
    folder: 'OnQuota'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
