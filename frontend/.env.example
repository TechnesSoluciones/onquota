# PUBLIC (exposed to browser)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=OnQuota
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PRIVATE (server-side only)
# Add any server-side only secrets here
